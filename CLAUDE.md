# CLAUDE.md — civilization-sim

自主文明模拟系统。以东亚地理的统计与拓扑性质为舞台，从前文明期自主演化数千年，产出一个可查询、可重放、可分叉的历史数据库。

**当前阶段：EXP-01 已收尾并冻结为参考基线（commit `20da486`）；EXP-02 方案待批准，批准前不写代码。**

**唯一生效的实验规格是 [`docs/EXP-01-SPEC.md`](docs/EXP-01-SPEC.md)。** `docs/spec/` 四份规格已降为参考文档，互不相容，不作为实现依据。

**EXP-01 已验证的范围只有**：同版本运行确定性、**内存中**快照续跑、区块隔离、整数守恒、遍历顺序无关、运行身份与状态摘要分离。
**不得宣称**：跨版本存档兼容（无序列化格式、无 schema 版本、无升级路径，完全没测过）、人口学标定（8/9 假设为 D 级，无外部标定目标）、跨架构一致、多线程确定性、灭绝通路可达性。

## 先读什么

1. [`docs/EXP-01-SPEC.md`](docs/EXP-01-SPEC.md) — **唯一生效的实验规格**（已冻结）。动代码前必读。
1b. [`docs/EXP-02-PROPOSAL.md`](docs/EXP-02-PROPOSAL.md) — 下一切片的方案，**待批准**。
2. [`docs/EXP-01-CONFLICTS.md`](docs/EXP-01-CONFLICTS.md) — 旧文档哪些条款被取代、哪些冲突被推迟、七处过强结论的修正。
3. [`docs/CONSTITUTION.md`](docs/CONSTITUTION.md) — 项目宪法。原则仍是最高约束，但若干表述已修正（文件内有"此处修正"标注）。
4. [`docs/MANDATE.md`](docs/MANDATE.md) — 用户原始纲领存档，不修改。
5. [`research/`](research/) — 22 份学科简报 + 7 份红队审查 + 证据总账。**只用于写规则，不得用于写 prompt。**

`docs/spec/`、`docs/DECISIONS.md`、`docs/INTEGRATION-REVIEW.md` 是论证与备选方案的来源，查阅可以，**不要照着实现**。

## 每次会话必须遵守

**证据纪律**
- 等级：**A** 多来源实证且有量化参数 / **B** 理论共识但量化弱 / **C** 有实质争议或单一来源 / **D** 无来源、我们自己的假设。
- 不许编造文献、数字、DOI。引用要能指回 `research/` 的具体位置（`brief-slug §小节` 或 `critique-slug F编号`）。
- 自己的推断显式标注"本文原创，无来源，D 级"。
- 找不到参数就写"文献未提供可用参数"，**不要给一个看起来合理的数字**。
- **"我们没获取到" ≠ "不存在"，更 ≠ "作者未公开"。** 403/404/付费墙是我们的取用失败，应记为待办的取用任务。
- **证据等级只用于文档与报告，永不作运行时开关。** 不得出现"第 N 年起某等级的机制才生效"这类阶段门。

**四态严格区分（任何一处合并都是 bug）**
`NOT_IMPLEMENTED`（没写，禁止当 0 参与计算）/ `UNCALIBRATED`（能跑，下游结论带标记）/ `UNKNOWN`（世界的一等状态，禁止静默替换为"不存在"）/ `NO_EFFECT`（是结论，必须有测量支撑）。
未做完的子系统返回 `NOT_IMPLEMENTED`，**不是**"无效应"，也**不是**一个看起来合理的近似值。

**设计纪律**
- 每提出一个机制/接口/数据结构，同时写清**它禁止了什么**。
- 内核只承认：物质与能量存量、人、位置、关系边、信息副本。
- 原语准入：定义不引用任何制度、参数全是物理量/信息量/关系边。"征税"不合格，"以暴力威胁索取资源转移"合格。
- **多个主体争夺同一资源时，必须同时结算**：各自独立提出申请 → 统一裁决冲突 → 一次性提交。顺序执行即使可复现，也只是把遍历顺序变成一个隐藏参数。EXP-01 的采集（§4.2）与分裂（§4.6）都栽过这个跟头。
- **完整状态比较**：确定性哈希必须覆盖**一切影响后续演化的字段**（EXP-01 的教训：漏了 `mem`/`memt`，整整一类只污染记忆的 bug 能安静通过）。字段集中在一处定义，新增字段只改那一处。

**红线（构建期错误，不是风格问题）**
- 浮点进入世界状态 → 定点整数
- 全局自增计数器分配实体 id → 内容/血缘派生哈希
- 全局有状态 PRNG → counter-based，key=(world_seed, stream_id, tick, entity_id, slot)
- 逆变换采样做离散决策 → Gumbel-max（EXP-01 尚无多候选决策，引入时生效）
- 遍历哈希容器、无确定性 tiebreaker 的 argmax
- LLM 写入世界状态、因果图、任何数值、任何 `name` 字段
- 内核 import 观察层；agent 决策模块 import 世界状态模块
- 宏观指标当触发器（`if PSI>0.8: trigger_rebellion()`）
- 以真实地名/区域名为键的常量字典
- `legitimacy`/`stability`/`prosperity` 这类无微观所指的宏观标量字段
- `enum PolityStage`、`enum Class`、`tech_tree.yaml`
- 目标导向调节机制：clamp、拒绝采样、灭绝兜底、事件冷却、存在性保证、自适应速率控制器（完整清单见宪法 §3.1；确需保留的带 `# GATED(...)` 注释，CI 计数只降不升）

**LLM 的位置**（EXP-01 内为零，接入前必须回到这一节）
- **测度归规则，分布归 LLM，采样归我们的 PRNG。** LLM 输出的是选择集上的一个分布，不是一次选择。
- 污染只用 `D_KL(p_LLM ‖ π)` 度量。**`Σ −log2 π(a*)` 不是偏差度量**——`p_LLM = π` 时它的期望等于 `H(π) > 0`，当预算用会把零偏差报成有偏差，还奖励把 `π` 做尖。
- 接入前必须跑 `p_LLM ≡ π` 的 mock：所有污染指标必须读到 0，读不到就是指标自己有 bug，不得上线。
- "不作为"是规则侧默认路径，不是选项之一。
- 世界在 LLM 完全关闭时必须能独立跑完全程。

**验证纪律**
- 能做成二值结构检验的，不要做成统计检验。
- **必过测试失败时，测试进程必须返回非零退出码。**
- 每个二值检验配一个错误注入版本，且**必须实测它真的会红**。没红的不要机械删除——查清为什么，写下它在什么条件下才承重（EXP-01 有两个这样的：`poison-clamp` 空转、`poison-float` 目标性质超出范围）。
- **长跑覆盖不到的路径必须有定向场景测试**（EXP-01 的教训：分裂冲突解决在 175 次申请里撞车 0 次，挂在长跑上的测试永远绿）。
- 任何"agent 层解释了历史"的主张，必须附带与同承载力零模型的对比。
- 参数改动分类为 `bug` / `evidence`（附引文）/ `aesthetics`（结果不好看），aesthetics 计数公开。
- **未执行的测试不得写成已通过。** 验收清单必须区分"已实测"与"未执行"，并写明未执行的原因。

**表述纪律**
- 不得为了让输出好看而改机制或参数。
- **枯燥不是正确性的证据。** 枯燥与正确性之间两个方向都没有推断关系；判断机制对不对只能靠机制自己的检验（守恒、单位、量级、错误注入）。
- "世界什么都没发生"是合法的科学结果。
- 不要把"我没写这条规则"当作涌现的证明。
- **类型名与函数名不构成涌现依据。** 改名不改变实质；判据是**内核是否读取那个标签来改变行为**。命名黑名单只是代码评审提示。
- `P/T`（自由参数数 / 外部标定目标数）是**提示指标，不是判据**。可证伪性取决于约束的结构，不是数量比。

## 什么时候必须停下来问人

- 要改 `docs/EXP-01-SPEC.md` 的规则、单位或更新顺序
- 要改 `docs/CONSTITUTION.md` 第 2 章任何一条
- 要为了让结果"更合理/更有特色"而调参数或改地图
- 要修改或扩展 EXP-01（它已冻结为基线，commit `20da486`）
- 要动 EXP-01 的已知限制（默认样本无灭绝、迁移损失恒为零）——这些会改变世界形态，不是 bug 修复
- 要在 EXP-02 方案获批之前写任何 EXP-02 的代码
- 要启用被禁用清单覆盖的任何机制

## 写文档时

克制。不要为了形式创建空文档。详细内容放进对应设计文档，由上层文档引用。重要设计决定写 ADR（`docs/adr/`）：决定了什么 / 为什么 / 依据是什么 / 有哪些替代方案 / 哪些部分仍是待验证假设。
