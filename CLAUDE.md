# CLAUDE.md — civilization-sim

自主文明模拟系统。以东亚地理的统计与拓扑性质为舞台，从前文明期自主演化数千年，产出一个可查询、可重放、可分叉、可做反事实实验的历史数据库。

**当前阶段：Phase 0（建模前研究与奠基）已完成第一轮，等待人类审核。未经签字不得开始内核实现。**

## 先读什么

1. [`docs/CONSTITUTION.md`](docs/CONSTITUTION.md) — 项目宪法。最高约束，冲突时以它为准。**每次涉及设计决策前必读。**
2. [`docs/MANDATE.md`](docs/MANDATE.md) — 用户原始纲领存档，不修改。
3. [`docs/spec/`](docs/spec/) — 四份工程规格（状态本体 / 因果重放 / LLM 边界 / 验证体系）。
4. [`docs/DECISIONS.md`](docs/DECISIONS.md) — 决策登记表，A 档是写第一行内核代码前必须定死的。
5. [`research/`](research/) — 22 份学科简报 + 7 份红队审查 + 证据总账。**只用于写规则，不得用于写 prompt。**

## 每次会话必须遵守

**证据纪律**
- 全项目统一证据等级：**A** 多来源实证且有量化参数 / **B** 理论共识但量化弱 / **C** 有实质争议或单一来源 / **D** 无来源、我们自己的假设。
- 不许编造文献、数字、DOI。凡引用必须能指回 `research/` 里的具体位置，写成 `brief-slug §小节` 或 `critique-slug F编号`。
- 自己的推断必须显式标注"本文原创，无来源，D 级"，不得写成历史规律。
- 找不到参数就写"文献未提供可用参数"，**不要给一个看起来合理的数字**。

**设计纪律**
- 每提出一个机制/接口/数据结构，必须同时写清**它禁止了什么**。一个不禁止任何东西的设计等于没有设计。
- 内核只承认：物质与能量存量、人、位置、关系边、信息副本。**不存在** `Polity`/`Settlement`/`Dynasty`/`Class`/`Religion` 类型；这些范畴只存在于观察层检测器与 agent 信念中。
- 原语准入：定义不引用任何制度、参数全是物理量/信息量/关系边才允许。"征税"不合格，"以暴力威胁索取资源转移"合格。
- 未达证据门槛的子系统，对外接口返回"不存在/无效应"，**不得返回看起来合理的近似值**。占位实现从不被删除，它们会成为世界的物理定律。

**红线（这些是构建期错误，不是风格问题）**
- 浮点进入世界状态 → 用定点整数
- 全局自增计数器分配实体 id → 用内容/血缘派生哈希
- 全局有状态 PRNG → 用 counter-based，key=(world_seed, stream_id, tick, entity_id, slot)
- 逆变换采样做离散决策 → 用 Gumbel-max
- 遍历哈希容器、无确定性 tiebreaker 的 argmax
- LLM 写入世界状态、因果图、任何数值、任何 `name` 字段
- 手写 `emit_event()` 命名事件（事件由语义无关的重大性泛函自动提名）
- 内核 import 观察层；agent 决策模块 import 世界状态模块
- 宏观指标当触发器（`if PSI>0.8: trigger_rebellion()`）
- 以真实地名/区域名为键的常量字典
- `legitimacy`/`stability`/`prosperity` 这类无微观所指的宏观标量字段
- `enum PolityStage`、`enum Class`、`tech_tree.yaml`
- 目标导向调节机制：clamp、拒绝采样、灭绝兜底、事件冷却、存在性保证、自适应速率控制器（完整清单见宪法 §3.1；确需保留的必须带 `# GATED(...)` 注释，CI 计数只降不升）

**LLM 的位置**
- 测度归规则，选择归 LLM：规则拥有每一个概率测度与每一次状态转移，LLM 只能从规则生成的有限选择集里挑元素，外加不参与数值计算的标签文本。
- "不作为"是规则侧默认路径，不是选项之一。
- 世界在 LLM 完全关闭时必须能独立跑完三千年。
- prompt 里禁止出现真实地名、真实历史专名、经纬度绝对值、`research/` 里的任何 ≥8 字连续片段。

**验证纪律**
- 凡是能做成二值结构检验的，不要做成统计检验。统计判据能被调参绕过，结构检验不能。
- 任何"agent 层解释了历史"的主张，必须附带与同承载力零模型的对比。
- 任何反事实结论必须附带空干预噪声地板与反事实有效期。
- 参数改动必须分类为 `bug` / `evidence`（附引文）/ `aesthetics`（结果不好看）。aesthetics 计数公开。
- 每次超过 100 模拟年的运行都要进运行登记表，无论结果好坏。

**表述纪律**
- 模拟层的输出会是枯燥的，**枯燥是正确性的证据**。不要为了让它好看而改任何东西。
- "世界什么都没发生"是合法的科学结果，不是 bug。
- 不要把"我没写这条规则"当作涌现的证明。

## 什么时候必须停下来问人

- 要修改 `docs/CONSTITUTION.md` 第 2 章任何一条（= 全部已跑历史线作废）
- 要动 A 档决策（`docs/DECISIONS.md`）
- 要为了让结果"更合理/更有特色"而调参数或改地图
- 要启用 `world_external_flux` 或任何被禁用清单覆盖的机制
- 自主性、`budget_sensitivity`、`causal_purity` 等指标越界

## 写文档时

克制。不要为了形式创建空文档。详细内容放进对应设计文档，由上层文档引用。重要设计决定写 ADR（`docs/adr/`），必须包含：决定了什么 / 为什么 / 依据是什么 / 有哪些替代方案 / 哪些部分仍是待验证假设。
