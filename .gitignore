# 运行产物（Phase 1 之后）
/runs/
/out/
*.log

# 系统
.DS_Store

# Python
__pycache__/
*.pyc

# OBS-01 观察台的运行期数据目录（SQLite + 各次运行的逐年记录）。
# 预生成案例在 observer/preset/ 里跟着版本库走，这里只放本机跑出来的东西。
observer/data/
observer/data_test/
observer/tests/web/_poison_app.js

# Cockpit 薄接力的运行期状态（队列、日志、STATUS），不进版本库
.dispatch/

# 动漫案例的预置数据：由 python3 -m observer.make_anime_cases 现做（确定性重算，几秒钟），
# 四条 300 年的逐年记录二十多兆，不进版本库；清单与核验脚本在 docs/ 里跟着走。
observer/preset/preset-anime-*/
