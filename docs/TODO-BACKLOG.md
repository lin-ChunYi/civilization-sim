# 待办登记（非阻塞）

放在这里的都是**已知、当前不影响模拟正确性 / 数据完整性 / 安全**的小问题。
记下来是为了不丢，也为了不因为它们再停下整个项目。真正影响上述三项的问题不进这张表，直接修。

| # | 事项 | 归属 | 为什么可以先放着 |
|---|---|---|---|
| 1 | 工作进程**心跳**：一个还活着但卡死的进程仍会占着唯一的任务槽，只能手动取消 | 后台 | 死进程已能自动回收（O19/O23）；卡死是罕见情形，且有取消按钮兜底，不会丢数据 |
| 2 | 页面顶栏"模型"读的是 `/api/config` 的**默认引擎**，打开 `exp04` 的运行时仍显示 `exp03/verify3.py` | UI 分支 | 正确来源 `run.engine` / `run.engine_path` 从 obs-1.0 起就在 run 行里，已写进契约 §6；纯显示问题 |
| 3 | 逐年记录是全量快照（300 年约 1.5 MB），`exp04` 的交换事件还会让事件数上升；没有压缩或分片 | 后台 | 有年数、运行条数、数据目录三道上限挡着；单用户规模够用 |
| 4 | EXP-04 的 G1 反直觉读数（共享越多、离全知臂越远）**没有解释** | 实验 | 解释需要专门的定向实验（例如统计同一目标格的并发迁入），**未获批准不做**；现在只保留原始读数 |
| 5 | 浏览器回归依赖前端的 `window.__obs` 钩子与若干 DOM id | 两边 | UI 改版后钩子若不在，回归会如实记为**未覆盖**（不假装通过，也不把 UI 分支判红），契约 §3 有约定 |
| 6 | `exp04/` 尚未冻结 | 实验 | 等审阅结论；冻结后按老规矩转为只读并登记基线 commit |
| 7 | 线上部署未执行 | 部署 | 配置齐备（`render.yaml` / Dockerfile），缺账号授权与付费实例决定 |
