# ART-PACK-001 processed sprites

Source candidates stay read-only at `/Users/ecool/Desktop/civilization/art-assets/grok-imagine-20260912`.
Style lock: variant-01, painterly miniature, ochre/moss/dark-brown.

White JPG sheets are not used on the map. These PNGs are flood-knocked, cropped, feet-aligned, and clipped to hexes in `app.js`.

- Characters (`char-*`) are group representatives, not biographies.
- Walk (`walk-a`, `walk-b`) is two approximate poses plus CSS bob/swap, not a verified loop.
- Hex tiles decorate ledger resource / blocked / memory values. Grass is not a crop type.
- Portraits (`port-*`) are dossier display only.
- `menu-bg.jpg` is the 创世/运行 tab illustration, not the replay map.
- `prop-*` are decorative camp props, not cities or buildings.
- Event pair images still anchor `e.cell`. Split still has no map locate.
